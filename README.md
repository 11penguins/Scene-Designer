# Scene-Designer project page

Static project page built from the figures and text in `AuthorKit27.zip`.

## Local preview

```bash
python -m http.server 8080 --directory project_page
```

Open `http://localhost:8080`.

The public project page is deployed with GitHub Pages. Replace anonymous citation
metadata and enable the Code link after de-anonymization and code release.
